[project]
name = "ai-agent"
version = "0.1.0"
description = "A beginner-friendly AI-powered terminal chat agent using Python, LangChain, and OpenRouter."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langchain>=0.3.26",
    "langchain-openai>=0.3.28",
    "langgraph>=0.5.4",
    "python-dotenv>=1.1.1",
]
